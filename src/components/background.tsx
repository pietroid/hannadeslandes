import * as React from 'React';
import styled from 'styled-components';

const Background = () => {
    return (<>
        <UniverseBackground/>
        <UniverseBackground/>

        <UniverseBackground/>
        </>
    );
}

const UniverseBackground = styled.div`
`

export default Background;